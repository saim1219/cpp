#include<iostream>
using namespace std;
int main()
{
    //----------------------------------------(Declaration)------------------------------------------
    //Declaration means that variable is only declared and memory is allocated, but no value is set. 
    int a;//this is declaration
    //----------------------------------------(Initializaion & Defination)------------------------------------------
    //However,giving value to the variable at the time of its declaration is called defination and initalization
    int b=10;//this is defination and initalization
    return 0;
}